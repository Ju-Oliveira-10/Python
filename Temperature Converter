#Exercise 4: Temperature Converter
#Use a function to convert Celsius → Fahrenheit and Fahrenheit → Celsius, and a while loop for menu control.

def C_to_F(c):
   return (c * 9/5)+32

def F_to_C(f):
   return (f - 32) * 5/9

while True:
   degrees = float(input("Enter the degrees: "))
   CF = input("Celsius 'C' or Fahrenheit 'F': ")
   if CF.lower() == "c":
      print("The degrees in F are:", C_to_F(degrees))
      break
   elif CF.lower() == "f":
      print("The degrees in C are:", F_to_C(degrees))
      break
   else:
      break
   